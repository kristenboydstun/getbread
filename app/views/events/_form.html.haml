%section.event_form
  / multistep form
  = form_for :event, url: { action: "create" }, html: { id: "msform" } do |f|
    / progressbar
    %ul#progressbar
      %li.active Setup
      %li Event Details
      %li Menu
    / fieldsets
    %fieldset
      %h2.fs-title Setup
      %h3.fs-subtitle
        = render "shared/errors", :obj => @event
      = f.text_field :name, placeholder: "Event name"
      = f.text_field :host_name, placeholder: "Host's name (optional)"
      /= f.text_field :host_email, placeholder: "Email (optional)"
      %input.next.action-button{:name => "next", :type => "button", :value => "Next"}/
    %fieldset
      %h2.fs-title Event Details
      /%h3.fs-subtitle Almost done!
      = f.text_field :location, placeholder: "Location or Address"
      = f.text_field :start_date, placeholder: "Date", 'data-behaviour' => "datepicker"
      /= f.text_field :description, placeholder: "Details (optional)"
      %input.previous.action-button{:name => "previous", :type => "button", :value => "Previous"}/
      %input.next.action-button{:name => "next", :type => "button", :value => "Next"}/
    %fieldset
      %h2.fs-title Menu
      %h3.fs-subtitle Option to choose a template menu coming soon!
      %input.previous.action-button{:name => "previous", :type => "button", :value => "Previous"}/
      %input.action-button{:name => "submit_button", :type => "submit", :value => "Done!"}/

/ jQuery easing plugin
%script{:src => "http://thecodeplayer.com/uploads/js/jquery.easing.min.js", :type => "text/javascript"}

